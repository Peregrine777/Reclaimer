import { defineConfig } from 'vite';

export default defineConfig({
  base: '/Reclaimer/', // The repo name is 'Reclaimer', update this if your repo name is different.
  build: {
    outDir: 'dist',
  },
});